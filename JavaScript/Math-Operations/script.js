alert("MOOO")

do{
var Radius = prompt("Enter Radius");
}while(!Radius || isNaN(Radius))

alert(Math.PI * Math.pow(Radius, 2));

do{
var value = prompt("Enter Value");
}while(!value || isNaN(value))

alert(Math.sqrt(value));

